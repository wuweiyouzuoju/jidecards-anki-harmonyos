# 历史实验

- `tmp-agent-live-probe.mjs`：历史 Provider 工具调用排查，会向真实服务发送脚本内样例并消耗调用额度；只在明确要求此类排查时运行，需要外部 API key，内容可能随协议过期。
- `verify-fix-plan.mjs`：一次性配色方案比较，不能替代当前主题回归。

从仓库根执行；这些脚本不属于自动验证，不随源码导出分发。当前开发入口见 [工具目录](../README.md)。
