# 编程 Agent

[返回任务索引](../../PROJECT_CONTEXT.md)

本页只约束负责修改 jidecards 仓库的编程 Agent。产品运行时的应用内 Agent 见 [应用内 Agent](agent.md)。

## 最高优先级

编程 Agent-first 是本项目最高开发优先级。每次开发都必须优先降低编程 Agent 理解、修改、验证和持续交付项目的成本。应用内 Agent 是产品运行时能力，按产品需求和现有安全边界维护，不得与编程 Agent 的开发治理混为一谈。

普通产品需求、历史计划和局部优化不得自动压过这个优先级。只有用户明确改变本次目标时，才允许暂时采用其他优先级；完成任务时仍要说明对 Agent-first 的影响。

## 开发闭环

1. 先读根 `AGENTS.md`、`PROJECT_CONTEXT.md` 和本任务领域文档，再检查当前工作树和调用点。
2. 修改必须保留清晰入口、稳定责任边界、可直接导入的业务模型和真实验证路径。
3. 新能力必须同时考虑源码入口、测试入口、错误路径、文档入口和后续 Agent 如何定位它。
4. 任务开始前按变更路径读取 `.agents/rules/paths/` 局部规则，并按 [开发任务契约](task-contract.md) 明确目标、责任、不变量和验证。
5. 领域测试用于快速反馈，结束前运行完整 `npm test`；跨模块、ArkTS 或原生变更按验证说明补齐 HAP、主机和设备验证。
6. 长期决策写入受版本控制的 `docs/decisions/`，不把 `.trae/` 或历史计划当作当前知识源。

## 名称边界

- 编程 Agent：负责阅读、修改、测试和交付本仓库的开发 Agent。
- 应用内 Agent：jidecards 运行时提供的 AI 制卡/改卡功能。
- 文档中单独写“Agent”时，必须根据上下文明确属于哪一种；任务入口、测试名和决策记录优先使用完整名称。

## 不得降低的能力

- 不得为了局部功能牺牲 Agent 可定位的模块边界、可测试业务规则或验证入口。
- 不得把关键规则只写进源码旁注、聊天记录、被忽略目录或历史计划。
- 不得用历史测试数量、旧版本说明或未完成 checkbox 代替当前源码和实际验证。
- 不得让新增复杂度没有责任表、验证命令或交接信息。
