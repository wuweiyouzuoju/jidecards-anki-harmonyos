# 记得闪卡 v2.3.3 官方公告实施计划

> 归档状态：这是一次性历史设计/执行记录，不是当前路线图、待办列表或操作手册。未勾选项不表示仍未实现；当前事实请查阅 [文档导航](../../README.md)、[开发状态](../../DEVELOPMENT_PLAN.md)、[当前架构](../../architecture.md) 和实际源码/测试。


**Goal:** 生成可直接上传的 2.3.3 双语官方公告，并把它取代旧版本更新弹窗的决定写入项目历史。

**Architecture:** 不修改公告拉取与弹窗代码。现有弹窗已经提供与旧欢迎弹窗一致的磨砂背景、圆角卡片和动画；本次只激活托管 JSON，并用现有协议限定版本 2.3.3、公告 ID 和展示时间。

**Tech Stack:** JSON、Node.js 契约测试、Markdown。

## Global Constraints

- 中文正文使用 `【新增】`、`【优化】`、`【修复】`；英文使用 `[New]`、`[Improved]`、`[Fixed]`。
- 云端牌组必须表述为 2.3.3 首次新增，不写“提升下载与导入稳定性”。
- 明确写出更多、更全的牌组可在官方 QQ 群 `726837065` 获取。
- 公告仅对 2.3.3 生效，不设置详情链接，文件不超过 5 KiB。
- 不上传、不替换或删除任何云端文件。

---

### Task 1: 发布公告清单并记录版本历史

**Files:**
- Modify: `tools/tests/official-announcement-flow-contract.test.mjs`
- Modify: `hosting/announcement.json`
- Modify: `PROJECT_CONTEXT.md`

**Interfaces:**
- Consumes: `解析可展示官方公告(jsonText, currentVersion, nowMs, language)` 的 schema v1 协议。
- Produces: 公告 ID `20260829-v2.3.3-release`，仅版本 2.3.3 可展示。

- [ ] **Step 1: 把 manifest 契约测试改为要求正式公告**

将原测试替换为：

```js
test('hosted announcement manifest publishes the v2.3.3 release notice under 5 KiB', () => {
  const text = read('../../hosting/announcement.json');
  const manifest = JSON.parse(text);
  assert.equal(manifest.schemaVersion, 1);
  assert.equal(manifest.announcement.enabled, true);
  assert.equal(manifest.announcement.id, '20260829-v2.3.3-release');
  assert.match(manifest.announcement.contentZh, /首次加入云端牌组下载/);
  assert.match(manifest.announcement.contentZh, /726837065/);
  assert.match(manifest.announcement.contentZh, /AnkiWeb/);
  assert.match(manifest.announcement.contentZh, /图片遮罩/);
  assert.doesNotMatch(manifest.announcement.contentZh, /提升大型牌组下载和导入的稳定性/);
  assert.match(manifest.announcement.contentEn, /Cloud deck downloads are here for the first time/);
  assert.equal(manifest.announcement.minimumAppVersion, '2.3.3');
  assert.equal(manifest.announcement.maximumAppVersion, '2.3.3');
  assert.equal(manifest.announcement.actionUrl, '');
  assert.ok(Buffer.byteLength(text, 'utf8') <= 5 * 1024);
});
```

- [ ] **Step 2: 运行测试确认 RED**

Run: `node --experimental-transform-types --import ./tools/tests/register-ts-hook.mjs --test tools/tests/official-announcement-flow-contract.test.mjs`

Expected: FAIL，因为 `hosting/announcement.json` 当前仍为停用状态。

- [ ] **Step 3: 写入正式公告 JSON**

写入：

```json
{
  "schemaVersion": 1,
  "announcement": {
    "id": "20260829-v2.3.3-release",
    "enabled": true,
    "titleZh": "记得闪卡 v2.3.3 更新",
    "contentZh": "【新增】首次加入云端牌组下载。进入软件即可选择并下载 6 套牌组：CET 四六级词汇、高考英语词汇、中考英语词汇、AI 机器学习、AI 计算机专业工具名词术语和中国法律专业版；下载完成后会自动导入，不用再手动找文件。更多、更全的牌组可加入官方 QQ 群 726837065 获取。\n\n【新增】新增官方公告。以后重要更新和通知会在首页及时送达，点击“我知道了”后，同一条公告不会重复打扰。\n\n【新增】云端牌组会显示卡片数量、版本和文件大小，支持多选、下载进度和逐个导入；失败项目可以重试，已经成功导入的不会重复处理。\n\n【优化】AnkiWeb 同步页新增账号说明和免费注册链接，第一次使用同步更容易上手。\n\n【修复】修复部分图片在创建图片遮罩卡片时无法读取或格式识别失败的问题。",
    "titleEn": "What's new in jidecards v2.3.3",
    "contentEn": "[New] Cloud deck downloads are here for the first time. Choose from six decks when entering the app: CET-4/6 Vocabulary, Gaokao English Vocabulary, Zhongkao English Vocabulary, AI & Machine Learning, AI & Computer Tools and Terms, and Chinese Law Professional. Downloads are imported automatically. For more complete deck collections, join the official QQ group 726837065.\n\n[New] Official announcements are now available. Important updates and notices will arrive on Home. After you tap “Got it,” the same announcement will not appear again.\n\n[New] Cloud decks show card count, version, and file size, with multi-select, download progress, and sequential import. Failed items can be retried without processing successful imports again.\n\n[Improved] The AnkiWeb sync page now explains account requirements and links to free registration, making first-time setup easier.\n\n[Fixed] Fixed cases where images could not be read or correctly recognized when creating image occlusion cards.",
    "publishedAt": "2026-08-29T00:00:00+08:00",
    "startsAt": "2026-08-29T00:00:00+08:00",
    "expiresAt": "2099-12-31T23:59:59+08:00",
    "minimumAppVersion": "2.3.3",
    "maximumAppVersion": "2.3.3",
    "actionUrl": ""
  }
}
```

- [ ] **Step 4: 更新项目历史**

在 `PROJECT_CONTEXT.md` 的“关键设计决策”增加：自 2.3.3 起版本更新内容由云端官方公告承载；旧欢迎弹窗保持静默；公告沿用旧弹窗标签格式与视觉外壳；每个版本使用新 ID；云端牌组首次发布文案指向 QQ 群 `726837065`。

- [ ] **Step 5: 验证 GREEN**

Run:

```powershell
node --experimental-transform-types --import ./tools/tests/register-ts-hook.mjs --test tools/tests/official-announcement-flow-contract.test.mjs
npm test
npm run build:app -- -SkipRust
git diff --check
```

Expected: 公告契约测试通过；全量测试 550 项或更多全部通过；应用构建成功；无空白错误。

- [ ] **Step 6: 提交**

```powershell
git add -- hosting/announcement.json tools/tests/official-announcement-flow-contract.test.mjs PROJECT_CONTEXT.md
git commit -m "release: publish v2.3.3 announcement manifest"
```
