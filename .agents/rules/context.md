# 项目知识维护

PROJECT_CONTEXT.md 只承担入场索引：全局不变量、唯一事实来源、任务到领域文档/实现/验证的路由。
领域说明放 docs/development，按需读取；不要把每次修复记录追加到入口。
版本/SDK/发布开关从配置读取，测试数量和验证日期写任务报告或发布记录，不维护多份“当前值”。
修改模块边界时更新对应领域文档与入口路由；历史计划不能替代源码现状。文档链接由仓库测试验证。
